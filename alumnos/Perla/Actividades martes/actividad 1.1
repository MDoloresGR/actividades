rango = []
inicio = 1
mitad = 50
final = 100
intentos = 0
respuesta = ""
numero = 1
while numero <= 100:
  rango.append(numero)
  numero = numero + 1
print("piensa un número entre 1 y 100 ")
while len(rango) > 2:
  respuesta = input("¿tu número está entre el " + str(inicio) + " y el " + str(mitad) + "? S/N ")
  if (respuesta == "s" or respuesta == "S"):
    del rango[mitad:final]
    print(rango)
  elif (respuesta == "n" or respuesta == "N"):
    del rango[inicio-1:mitad]
    print(rango)
  else: 
    print("respuesta incorrecta, trata de nuevo")
    continue
  inicio = rango[0]
  final = rango[len(rango) - 1]
  medio = int(len(rango) / 2)
  mitad = rango[medio - 1]  
  print(inicio)
  print(mitad)
  print(final)  
  intentos = intentos + 1 
print(intentos)  